一个爬图片的Node.js爬虫


待完善缺点：
1、没有递归并发控制，运行时间过长的话，进行递归的路线可能过多（超过100）
2、缺少一个判断网页是否已经被爬过的记录栈


切换到文件目录：
npm install   //引入依赖

node app //运行

打开app.js

更改里面的变量 url （目标网站）= "http||https + ://域名"

运行前在文件目录新建空文件夹images来存储图片
